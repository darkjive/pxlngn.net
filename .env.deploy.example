# IONOS Deployment Configuration
# Kopieren Sie diese Datei zu .env.deploy und tragen Sie Ihre Zugangsdaten ein:
# cp .env.deploy.example .env.deploy

###############################################################################
# IONOS FTP/SFTP Zugangsdaten
###############################################################################

# FTP/SFTP Server (z.B. your-domain.de oder ftp.ionos.de)
DEPLOY_HOST="your-ftp-server.ionos.de"

# FTP/SFTP Benutzername
DEPLOY_USER="your-username"

# FTP/SFTP Passwort
# WICHTIG: Diese Datei NICHT ins Git committen! (.gitignore prüfen)
DEPLOY_PASSWORD="your-password"

# Port (21 für FTP, 22 für SFTP)
DEPLOY_PORT=21

# Protokoll: "ftp" oder "sftp"
# SFTP ist sicherer und wird empfohlen, wenn verfügbar
DEPLOY_PROTOCOL="ftp"

# Remote-Pfad auf dem Server (meist /htdocs oder /)
# Dies ist das Zielverzeichnis, wo die Dateien hochgeladen werden
DEPLOY_REMOTE_PATH="/htdocs"

###############################################################################
# Build-Konfiguration
###############################################################################

# Lokales Build-Verzeichnis (Standard: dist)
BUILD_DIR="dist"

# Dry Run: Wenn true, wird nur angezeigt was gemacht würde, ohne tatsächlich hochzuladen
# Nützlich zum Testen
DRY_RUN=false

###############################################################################
# Optional: Website-URL für Post-Deployment Check
###############################################################################

# URL Ihrer Website für automatische Überprüfung nach dem Deployment
# Beispiel: https://www.ihre-domain.de
DEPLOY_SITE_URL=""

###############################################################################
# IONOS-spezifische Hinweise
###############################################################################

# 1. FTP-Zugangsdaten finden:
#    - Melden Sie sich im IONOS Control Panel an (https://my.ionos.de)
#    - Navigation: Hosting → Webspace & Domains → Ihre Domain
#    - Klicken Sie auf "FTP-Zugang einrichten"
#
# 2. SFTP verwenden (empfohlen):
#    - SFTP ist sicherer als FTP
#    - Gleiche Zugangsdaten wie FTP
#    - Port: 22
#    - DEPLOY_PROTOCOL="sftp"
#
# 3. Document Root:
#    - Bei den meisten IONOS-Paketen ist dies /htdocs
#    - Bei Subdomains kann es abweichen, z.B. /htdocs/subdomain
#
# 4. Mehrere Domains:
#    - Wenn Sie mehrere Domains haben, achten Sie auf den richtigen Pfad
#    - Beispiel: /htdocs/domain1.de, /htdocs/domain2.de
#
# 5. Sicherheit:
#    - Fügen Sie .env.deploy zur .gitignore hinzu (bereits enthalten)
#    - Teilen Sie diese Datei niemals öffentlich
#    - Verwenden Sie starke, eindeutige Passwörter
