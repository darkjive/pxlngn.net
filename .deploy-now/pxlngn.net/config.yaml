# .deploy-now/config.yaml

deployment:
  bootstrap:
    excludes:
      - .git
      - .github
      - node_modules
      - .deploy-now

  recurring:
    excludes:
      - .git
      - .github
      - node_modules
      - .deploy-now

project:
  name: pxlngn.net
  type: static
  build:
    command: npm run build
    output: dist
  environment:
    NODE_VERSION: 20
